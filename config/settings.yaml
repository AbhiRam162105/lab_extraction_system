processing:
  workers: 10
  batch_size: 100
  max_retries: 3
  timeout: 300

storage:
  type: "local"
  bucket: "lab-reports"
  base_path: "storage"

database:
  url: "sqlite:///./lab_extraction.db"

gemini:
  model: "gemma-3-27b-it"
  rate_limit: 10

redis:
  url: "redis://localhost:6379/0"
